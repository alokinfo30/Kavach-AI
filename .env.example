# Application Environment Variables
PORT=3000
NODE_ENV=development
JWT_SECRET=super-secret-key-change-in-production

# Optional: Point static deployments (e.g. Netlify/Vercel) to a remote backend API
# If omitted or if backend returns 404, built-in client-side mock engine will automatically activate
VITE_API_URL=
